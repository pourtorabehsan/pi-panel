/**
 * Package entry point — pi labels a package extension after its entry file's
 * parent directory, so the entry lives at the repo root to display "pi-panel"
 * (instead of "src") in the startup resources list.
 */
export { default } from "./src/index.ts";
